# Оптимизация прошивки для ESP32 с малым объемом памяти

## ❗ Проблема

Ошибка при компиляции:
```
Sketch too big; see https://support.arduino.cc/hc/en-us/articles/360013825179
```

ESP32 с ограниченной памятью не может вместить прошивку с поддержкой BLE + WiFi одновременно.

---

## ✅ Решение: используйте ТОЛЬКО BLE

**BLE (Bluetooth Low Energy)** - это основной и рекомендуемый способ подключения для VibeMon.

### Преимущества BLE:
- ✅ Меньше потребляет памяти (~150KB меньше чем WiFi)
- ✅ Меньше расход энергии
- ✅ Простое подключение
- ✅ Работает на всех смартфонах
- ✅ Не требует настройки WiFi

### 🎯 Настройка (по умолчанию)

**Шаг 1:** В файле `vibemon_esp32_advanced.ino`:
```cpp
#define WIFI_ENABLED false  // false = только BLE
```

**Шаг 2:** В файле `wifi_config.h`:
```cpp
#define WIFI_MODE_ENABLED false
```

**Всё!** Прошивка теперь использует только BLE и поместится в память.

---

## 🔌 Подключение через BLE

1. Загрузите прошивку с `WIFI_ENABLED false`
2. Откройте мобильное приложение VibeMon
3. Нажмите **"Найти ESP32 (BLE)"**
4. Выберите **"VibeMon-001-Pro"**
5. Готово! ✅

---

## 📊 Размеры прошивки

| Режим | Размер | Вмещается? |
|-------|--------|------------|
| Только BLE (рекомендуется) | ~800KB | ✅ Да |
| BLE + WiFi | ~950KB | ❌ Нет (на некоторых ESP32) |
| Только WiFi | ~870KB | ⚠️ Может не вместиться |

---

## 🆘 Если всё равно не помещается

### Оптимизация 1: Отключите отладку
В `vibemon_esp32_advanced.ino`:
```cpp
#define DEBUG_MODE false  // Экономит ~15KB
```

### Оптимизация 2: Уменьшите FFT буфер
```cpp
#define SAMPLES 128  // Было 256, экономит ~50KB
```
⚠️ **Внимание:** снизится частотное разрешение с 4 Гц до 8 Гц

### Оптимизация 3: Схема разделения памяти
В Arduino IDE:
```
Tools → Partition Scheme → No OTA (2MB APP / 2MB SPIFFS)
```

### Оптимизация 4: Компилятор
В Arduino IDE:
```
Tools → Core Debug Level → None
Tools → Optimize → Optimize for size (-Os)
```

---

## 🔄 Если нужен WiFi

Если вам критически важен WiFi и не хватает памяти:

### Решение: Купите ESP32 с большим объёмом Flash

Рекомендуемые модели:
- **ESP32-WROOM-32** (4MB) ✅ ~300₽
- **ESP32-WROVER** (4MB+) ✅ ~400₽  
- **ESP32-S3** (8MB+) ✅ ~500₽

Избегайте:
- ESP32-WROOM-32U (2MB) ⚠️ Мало памяти
- ESP8266 ❌ Не поддерживается

---

## 💡 Рекомендация

### Используйте BLE - это оптимально!

**Для большинства случаев BLE идеален:**
- ✅ Быстро (обновления 1 раз в секунду)
- ✅ Стабильно  
- ✅ Экономно (памяти и энергии)
- ✅ Просто (не нужна настройка WiFi)
- ✅ Дистанция 10-15м (достаточно для мониторинга)

**WiFi нужен только для:**
- Удалённого доступа через интернет
- Большой дистанции (50+ метров)
- Интеграции с веб-серверами

---

## ❓ FAQ

**Q: Почему BLE лучше WiFi?**  
A: BLE потребляет меньше памяти и энергии, проще в настройке. Для локального мониторинга этого более чем достаточно.

**Q: Какая дальность у BLE?**  
A: 10-15 метров в помещении. Этого хватает для промышленного мониторинга - датчик обычно крепится на оборудование, а оператор рядом.

**Q: Можно ли переключаться между BLE и WiFi?**  
A: Да, просто измените `WIFI_ENABLED` и перезалейте прошивку. Но использовать одновременно не получится на маленьких ESP32.

**Q: WiFi быстрее чем BLE?**  
A: Да, WiFi ~2 обновления/сек, BLE ~1 обновление/сек. Но для вибрационного мониторинга это не критично.

**Q: Что будет если прошивка всё равно не помещается?**  
A: Попробуйте все оптимизации выше. Если не помогает - купите ESP32 с 4MB памяти за ~300₽.

---

## 🔧 Проверка размера

После компиляции в Arduino IDE смотрите:
```
Sketch uses XXXXX bytes (XX%) of program storage space
```

**Должно быть < 1310720 bytes (< 80%)**

Если больше - применяйте оптимизации выше! 🚀
