/*
 * WiFi Configuration для VibeMon
 * 
 * ============================================================
 * ВАЖНО ДЛЯ ЭКОНОМИИ ПАМЯТИ:
 * ============================================================
 * 
 * По умолчанию используется только BLE (Bluetooth)
 * 
 * Если нужен WiFi (требует ~80KB дополнительной памяти):
 * 1. В vibemon_esp32_advanced.ino: #define WIFI_ENABLED true
 * 2. Укажите ниже имя и пароль вашей домашней WiFi
 * 3. ESP32 подключится к вашей WiFi сети
 * 4. IP адрес будет показан в Serial Monitor (115200)
 * 5. Используйте этот IP в мобильном приложении
 * 
 * ============================================================
 */

#ifndef WIFI_CONFIG_H
#define WIFI_CONFIG_H

// ========== РЕЖИМ РАБОТЫ ==========
#define WIFI_MODE_ENABLED false  // false = только BLE (экономия памяти)

// ========== WiFi STATION (подключение к домашней WiFi) ==========
// 👇 ЗАМЕНИТЕ на ваши данные WiFi:
#define WIFI_SSID "YOUR_WIFI_NAME"          // ⚠️ Имя вашей домашней WiFi сети
#define WIFI_PASSWORD "YOUR_WIFI_PASS"      // ⚠️ Пароль от WiFi

// ========== TCP SERVER ==========
// Порт TCP сервера
#define WIFI_TCP_PORT 8888

// ========== ПРОТОКОЛ ==========
// Формат пакета данных:
// 
// Заголовок (4 байта): 0x56 0x49 0x42 0x45 ("VIBE")
// Температура (4 байта): float
// VibrationData (32 байта): структура
//   - rms (float)
//   - rmsVelocity (float)
//   - peak (float)
//   - peakToPeak (float)
//   - crestFactor (float)
//   - dominantFreq (float)
//   - dominantAmp (float)
//   - status (uint8_t)
// Спектр (32 байта): 8 x float
// JSON статус (переменная длина): строка с \n
//
// Итого: 72 байта + JSON строка

// ========== КОМАНДЫ ==========
// Клиент может отправлять байт-команды:
// 0x01 - Перекалибровка датчика
// 0x02 - Сброс настроек
// 0x03 - Перезагрузка устройства

#endif // WIFI_CONFIG_H
